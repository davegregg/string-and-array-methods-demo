console.log("Is this JavaScript running?".length)

`A "property" is a named quality of an object.`

`A "method" is a function which has been designed to help us solve a common, general problem with a particular data type (strings or arrays, for example).`

`A "callback" is a function which has been provided as input to another function, so that it can be run LATER.`